# Vue 3 用户列表示例

本项目展示如何使用 Vue 3 从 `http://localhost:8080/users/all` 获取并展示用户列表。

快速开始：

```bash
cd /home/ubt/vue3/demo1
npm install
npm run dev
```

在浏览器打开 http://localhost:5173 （Vite 默认端口），确保后端接口在 `http://localhost:8080/users/all` 可访问并允许跨域。
